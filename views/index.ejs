<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include ("partials/header") %>
    <title>Home</title>
    <style>
      p.secondary {
        color: rgb(78, 78, 78);
      }
      div.outer {
        background-color: rgba(246, 246, 246, 0.85);
        width: 100%;
        height: 100vh;
        display: flex;
      }
      img {
        width: 100%;
        height: 100vh;
        position: absolute;
        z-index: -1;
      }
    </style>
  </head>
  <body>
    <%- include ("partials/nav") %>
    <div class="pic"><img src="note1.jpeg" alt="backgroundPhoto" /></div>
    <div class="outer p-5 mb-4 rounded-3">
      <div class="container-fluid py-5">
        <h1 class="display-5 fw-bold">貼文留言板</h1>
        <p class="col-md-8 fs-4">
          利用google帳號直接登入，或者本地註冊方式加入會員，可以看到其他人的貼文內容，也可建立自己的心情留言與其他好友分享。
        </p>
        <p class="secondary">
          練習使用express.js、EJS、MongoDB、Passport.js、Authentication、OAuth(Google
          Login Setup) 創建網頁，並透過render部署網路服務。
        </p>
        <%if(!user){%>
        <a class="btn btn-primary btn-lg" href="/auth/login">
          登入立即體驗！
        </a>
        <%}%> <%if(user){%>
        <a class="btn btn-primary btn-lg" href="/profile"> 進入我的個人檔案 </a>
        <%}%>
      </div>
    </div>
  </body>
</html>
